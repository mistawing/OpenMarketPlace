# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OpenMarketPlace' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for OpenMarketPlace
  pod 'SwiftGRPC'
  pod 'NIO'
  pod 'SwiftNIOHTTP1'
  target 'OpenMarketPlaceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'OpenMarketPlaceUITests' do
    # Pods for testing
  end

end
